# Optional: only set this if you actually add Gemini calls to the app.
# In Vercel, configure it in Project Settings > Environment Variables.
# Do not expose private API keys directly to client-side code.
GEMINI_API_KEY=""

# Optional public app URL for metadata, links, or callbacks.
APP_URL="http://localhost:3000"
